streamlit>=1.35.0
pandas>=2.0.0
plotly>=5.20.0
groq>=0.9.0
kaleido>=0.2.1

# Data ingestion — Excel (.xlsx) support
openpyxl>=3.1.0

# Analytics & forecasting (hard dependencies)
scikit-learn>=1.4.0
scipy>=1.12.0
numpy>=1.26.0

# Optional forecasters — install manually for better accuracy:
#   pip install prophet          (Facebook Prophet)
#   pip install statsmodels      (ARIMA)
# The app falls back to scikit-learn LinearRegression if these are absent.
